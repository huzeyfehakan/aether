[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "aether"
version = "0.1.0"
description = "Publisher Answer Integrity Layer MVP"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
